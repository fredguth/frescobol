<script> var myTracks=["http://soundcloud.com/radioguidance/hardboiled-blues-kid-who-remix","http://soundcloud.com/radioguidance/gyraxiom-feat-bernardo-ramos"];</script>
<p id="notice"><%= notice %></p>

<h1>Mix  <%= @mix.nome %> <small>por <%= @mix.autor %> </h1>
<div class="row">
<div class="span7">
	<h2> Tocar Mix</h2>
	<a href="#" id="frescobolPlayer" >Tocar</a>
	<h2> Tracks no Mix</h2>
	<table class="table table-striped">
		  <tr>
		    <th class="span2">id</th>
		    <th class="span2">URL</th>
		<!--     <th class="span2"></th> -->
		  </tr>
		
		<% @mix.tracks.each do |track| %>
		  <tr>
		    <td><%= track.id %></td>
		    <td><%= track.url %></td>
		     <!-- <td align="right">
          	 <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_track_path(track), :class => 'btn btn-mini' %>
          	 <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      track_path(track),
                      :method => :delete,
                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                      :class => 'btn btn-mini btn-danger' %>
        	</td> -->
      		</tr>
		 
		<% end %>
		</table>
	
	
	
	
<h2> Adcionar Track</h2>	
<%= render "tracks/form" %>

</div>
</div>

<%= link_to 'Editar', edit_mix_path(@mix) %> |
<%= link_to 'Voltar', mixes_path %>
